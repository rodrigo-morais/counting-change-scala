package recfun

object Main {
  def main(args: Array[String]) {
    println("Money is 4 to coins 1,2. The count change is: " + countChange(4, List.range(1, 2)))
  }
  
  def countChange(money: Int, coins: List[Int]): Int = {
    1
  }
}